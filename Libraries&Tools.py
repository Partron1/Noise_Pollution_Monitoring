# RPi.GPIO (for Raspberry Pi GPIO control)
# Flask or Django (for creating a backend server, if needed)

pip install Flask pyrebase4

# Run Flask App:

python app.py

# Edit config in app.py with your Firebase credentials
# requests (for API communication)
# OpenCV (if implementing camera-based parking detection)
# matplotlib (for plotting sensor data, if required)
# paho-mqtt (if using MQTT for IoT communication)
# tkinter or other GUI frameworks (for simple desktop app interfaces)